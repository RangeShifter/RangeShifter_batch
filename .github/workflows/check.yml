name: check
on: push

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: build
        run: g++ -o rs.exe ./src/*.cpp ./src/RScore/*.cpp

      - name: run
        run: |
          cd ./input_test/example
          ../../rs.exe
          
