name: check
on: push

jobs:
  example_matrix:
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]
        include:
          - os: windows-latest
            npm: ./build/Debug/RangeShifter.exe
          - os: ubuntu-latest
            npm: ./build/RangeShifter
          - os: macos-latest
            npm: ./build/RangeShifter
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node }}
      - if: ${{ matrix.npm }}
        run: npm install -g npm@${{ matrix.npm }}
      - run: npm --version
